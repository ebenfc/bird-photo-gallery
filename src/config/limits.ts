// Gallery curation limits
// Each species gallery is curated to a fixed number of photos.
// Users can photograph unlimited species, but each species gets limited gallery space.
export const SPECIES_PHOTO_LIMIT = 8;

// Maximum number of photos that can sit in the "inbox" (unassigned to any species).
// Prevents using unassigned photos as a workaround to bypass species limits.
export const UNASSIGNED_PHOTO_LIMIT = 24;
