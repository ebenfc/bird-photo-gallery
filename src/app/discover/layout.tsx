import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Discover | Bird Feed",
};

export default function DiscoverLayout({ children }: { children: React.ReactNode }) {
  return children;
}
